spring.application.name=notification-service
server.port=8083
server.shutdown=graceful

###### Database Configuration ######
spring.datasource.url=${NOTIFICATION_DB_URL:jdbc:postgresql://ep-curly-thunder-a1mscwgq-pooler.ap-southeast-1.aws.neon.tech/notification_db?user=neondb_owner&password=npg_iHvlGejoBa38&sslmode=require&channelBinding=require}
spring.datasource.username=${NOTIFICATION_DB_USERNAME:neondb_owner}
spring.datasource.password=${NOTIFICATION_DB_PASSWORD:npg_iHvlGejoBa38}
spring.jpa.open-in-view=false

###### RabbitMQ Configuration ######
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

###### Notification Service Configuration ######
notification.order-events-exchange=orders-exchange
notification.new-orders-queue=new-orders
notification.delivered-orders-queue=delivered-orders
notification.cancelled-orders-queue=cancelled-orders
notification.error-orders-queue=error-orders
notification.support-email=lem247358@gmail.com

############# Mail Properties ###########
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:lem247358@gmail.com}
spring.mail.password=${MAIL_PASSWORD:sadriidllldpwxdv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

######## Actuator Configuration  #########
management.info.git.mode=full
management.endpoints.web.exposure.include=*
management.metrics.tags.application=${spring.application.name}
management.tracing.enabled=false
management.tracing.sampling.probability=1.0